{
  "name": "back",
  "version": "1.0.0",
  "main": "app.ts",
  "license": "MIT",
  "scripts": {
    "dev": "deno run --import-map ./import_map.json --allow-net --allow-env --watch=./**/*.ts serve.ts"
  }
}
